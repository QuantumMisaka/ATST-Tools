from setuptools import setup, find_packages

setup(
    name="atst-tools",
    version="0.1",
    packages=find_packages(),
    package_dir={"": "."},
    install_requires=[],
    python_requires=">=3.9",
)